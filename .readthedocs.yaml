# Read the Docs configuration for nautobot-ssot-hudu
# https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"
  jobs:
    post_install:
      - pip install ".[docs]"

mkdocs:
  configuration: mkdocs.yml

formats:
  - htmlzip
